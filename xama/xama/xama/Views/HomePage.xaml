<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="xama.Views.HomePage"
             Title="{Binding Title}">
    <ContentPage.Content>
        <ListView ItemsSource="{Binding Films}" VerticalOptions="FillAndExpand" HasUnevenRows="true">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="10">
                            <FlexLayout JustifyContent="SpaceAround">
                                <Label Grid.RowSpan="2" HeightRequest="60" WidthRequest="60" Text="Temp Image"/>
                                <Label Grid.Column="1" Grid.Row="0" Text="{Binding Name}"/>
                                <Label Grid.Column="1" Grid.Row="1" Text="{Binding Description}"></Label>
                                <Label Grid.Column="1" Grid.Row="1" Text="{Binding Starring}"></Label>
                            </FlexLayout>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>